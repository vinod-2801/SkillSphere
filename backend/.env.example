# SkillSphere AI Backend Configuration
PORT=5000
NODE_ENV=development

# PostgreSQL Connection (Optional: in-memory prototype fallback is active when omitted)
# DATABASE_URL=postgres://postgres:password@localhost:5432/skillsphere
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=your_password
# PGDATABASE=skillsphere
